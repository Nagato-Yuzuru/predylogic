# Guides

!!! note "Outline — content in progress"
    Task-oriented how-to guides. Content is authored in Chinese first (the
    authoritative source); English translations follow. Use the language switcher.

Pick by task:

- **[Schema & Serde](serde.md)** — export a JSON Schema, load rules from a manifest, validate at config time.
- **[Hot Reloading](hot-reload.md)** — swap predicate logic at runtime; `PredicateHandle` and tombstones.
- **[Tracing](trace.md)** — customize `TraceStyle`, wire Trace into logging, control `short_circuit`.
- **[Composition & Performance](composition.md)** — `&` `|` `~` vs `all_of` / `any_of`, N-ary flattening, the bytecode fast path.
